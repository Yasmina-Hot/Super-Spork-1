{
  "name": "@spork/super-spork",
  "version": "0.1.0",
  "description": "SporkX CLI — unified agentic AI in your terminal (chat + code + agent + scan + memory + more)",
  "bin": {
    "sporkx": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "simple-git": "^3.24.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "keywords": ["ai", "cli", "spork", "openrouter", "agentic", "super-spork"],
  "license": "MIT"
}
